general {
        colors = true
        interval = 5
}

# order += "wireless _first_"
# order += "battery all"
order += "tztime local"

# wireless _first_ {
#         format_up = "W: (%essid) %ip"
#         format_down = "W: down"
# }


# battery all {
#         format = "%status %percentage %remaining"
# }

tztime local {
        format = "%H:%M %d/%m/%Y"
}
